{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://opencode.ai/schemas/skill-v1.json",
  "title": "SKILL.json v1",
  "description": "Schema for SKILL.json version 1 - OpenCode Skill Plugin canonical format\n\nAligned with:\n- ADR-0022: Platform-neutral Assistant Package Canonical and AgentNode Reference\n- ADR-0023: Parallel AgentNode Execution via Graph Fan-out and Fan-in",
  "type": "object",
  "required": ["version", "skill", "workflow"],
  "properties": {
    "version": {
      "type": "string",
      "const": "1",
      "description": "Schema version identifier"
    },
    "skill": {
      "type": "object",
      "required": ["id", "name", "description"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique skill identifier (kebab-case)"
        },
        "name": {
          "type": "string",
          "description": "Human-readable skill name"
        },
        "description": {
          "type": "string",
          "description": "Skill description"
        },
        "content_md": {
          "type": "string",
          "description": "Markdown content for generated SKILL.md"
        }
      }
    },
    "workflow": {
      "type": "object",
      "required": ["nodes", "edges"],
      "properties": {
        "nodes": {
          "type": "array",
          "description": "Workflow nodes",
          "items": {
            "$ref": "#/definitions/node"
          }
        },
        "edges": {
          "type": "array",
          "description": "Workflow edges (connections between nodes)",
          "items": {
            "$ref": "#/definitions/edge"
          }
        },
        "layout": {
          "type": "object",
          "description": "Canvas layout settings",
          "properties": {
            "grid_size": {
              "type": "integer",
              "description": "Grid size for snap-to-grid"
            },
            "snap_to_grid": {
              "type": "boolean",
              "description": "Whether snap-to-grid is enabled"
            },
            "viewport": {
              "type": "object",
              "properties": {
                "x": {
                  "type": "number",
                  "description": "Viewport X offset"
                },
                "y": {
                  "type": "number",
                  "description": "Viewport Y offset"
                },
                "zoom": {
                  "type": "number",
                  "description": "Viewport zoom level"
                }
              }
            }
          }
        }
      }
    },
    "policy": {
      "type": "object",
      "description": "Execution policy settings",
      "properties": {
        "execution_mode": {
          "type": "string",
          "enum": ["sequential", "parallel"],
          "description": "How nodes are executed"
        },
        "failure_mode": {
          "type": "string",
          "enum": ["fail_fast", "continue", "best_effort"],
          "description": "How failures are handled"
        }
      }
    }
  },
  "definitions": {
    "node": {
      "type": "object",
      "required": ["id", "name", "type", "position"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique node identifier"
        },
        "name": {
          "type": "string",
          "description": "Human-readable node name"
        },
        "type": {
          "type": "string",
          "description": "Node type (validation is domain-level, not schema-level)"
        },
        "position": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "minItems": 2,
          "maxItems": 2,
          "description": "Canvas position [x, y]"
        },
        "config": {
          "type": "object",
          "description": "Node-specific configuration\n\nADR-0022/0023: For Agent type nodes, config contains:\n- agent_ref: Reference to reusable Agent in package catalog\n- action_text: Action owned by AgentNode (required for Publish)\n- done_criteria: Done Criteria owned by AgentNode (required for Publish)\n- join_policy: Join policy for parallel execution (default: 'all')",
          "properties": {
            "agent_ref": {
              "type": "object",
              "description": "ADR-0022: Reference to reusable Agent in package catalog",
              "properties": {
                "package": {
                  "type": "string",
                  "description": "Package identifier"
                },
                "name": {
                  "type": "string",
                  "description": "Agent name within package"
                }
              },
              "required": ["package", "name"]
            },
            "action_text": {
              "type": "string",
              "description": "ADR-0020/0022: Action owned by AgentNode (required for Publish)"
            },
            "done_criteria": {
              "type": "string",
              "description": "ADR-0020/0022: Done Criteria owned by AgentNode (required for Publish)"
            },
            "join_policy": {
              "type": "string",
              "enum": ["all"],
              "description": "ADR-0023: Join policy for parallel execution (default: 'all' - all predecessors must complete)"
            }
          }
        }
      }
    },
    "edge": {
      "type": "object",
      "required": ["id", "source_node_id", "target_node_id", "branch"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique edge identifier"
        },
        "source_node_id": {
          "type": "string",
          "description": "Source node ID"
        },
        "target_node_id": {
          "type": "string",
          "description": "Target node ID"
        },
        "branch": {
          "type": "string",
          "description": "Branch condition (default, success, failure, etc.)"
        },
        "source_node_name": {
          "type": "string",
          "description": "Source node name (for display)"
        },
        "target_node_name": {
          "type": "string",
          "description": "Target node name (for display)"
        }
      }
    }
  }
}
